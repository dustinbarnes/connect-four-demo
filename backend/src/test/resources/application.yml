spring:
  datasource:
    url: jdbc:sqlite:../database/dev.db
    driver-class-name: org.sqlite.JDBC
  security:
    jwt:
      secret: "testsecretkeymustbeatleast32byteslong!!"
      expiration: 3600000